{
  "name": "super-brain-v2",
  "version": "2.0.0",
  "description": "Super Brain v2 - Intelligent API with dual brain architecture",
  "main": "src/server.py",
  "scripts": {
    "start": "python src/server.py",
    "train": "python -c "from src.utils.learner import Learner; from src.brain.dual import DualBrain; Learner.train('brain_dataset.jsonl', DualBrain())"",
    "init": "python -c "import os; os.makedirs('data', exist_ok=True)""
  },
  "dependencies": {
    "fastapi": "^0.95.0",
    "uvicorn": "^0.21.0",
    "python-dotenv": "^1.0.0",
    "sqlalchemy": "^2.0.0"
  }
}